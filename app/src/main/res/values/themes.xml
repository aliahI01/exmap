<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Exmap" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/hijaupastel</item>
        <item name="colorPrimaryVariant">@color/hijaupastel</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.AppCompat.Transparent" parent="android:Theme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="nightMode" parent="Theme.AppCompat.Light">
        <item name="colorControlActivated">@color/hijaudark</item>
        <item name="colorSwitchThumbNormal">@color/white</item>
        <item name="android:colorForeground">@color/toggle</item>
    </style>

    <style name="notiMode" parent="Theme.AppCompat.Light">
        <item name="colorControlActivated">@color/scholar</item>
        <item name="colorSwitchThumbNormal">@color/white</item>
        <item name="android:colorForeground">@color/toggle</item>
    </style>

    <style name="SCREEN">
        <item name="android:background">@color/hijaupastel</item>
    </style>
    <style name="TEXT">
        <item name="android:textColor">@color/hijaupale</item>
    </style>
    <style name="DESC">
        <item name="android:textColor">@color/hint</item>
    </style>
    <style name="HINT">
        <item name="android:textColor">@color/hijaupastel</item>
    </style>
    <style name="BUTTON">
        <item name="android:textColor">@color/hijaupale</item>
    </style>
    <style name="IMAGE">
        <item name="android:background">@drawable/round_img</item>
    </style>
    <style name="DIVIDER">
        <item name="android:background">@color/hijaupale</item>
    </style>

</resources>